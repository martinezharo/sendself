/**
 * The stylesheet and the fonts it names, for every document the site serves:
 * the app and landing page (main.tsx) and the static pages (site/static.ts).
 * One import, so the two can never be dressed differently.
 */
import "@fontsource-variable/bricolage-grotesque/wght.css";
import "@fontsource-variable/hanken-grotesk/wght.css";
import "@fontsource-variable/jetbrains-mono/wght.css";
import "./styles.css";
