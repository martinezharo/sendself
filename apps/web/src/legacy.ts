/**
 * Stable pre-rebrand identifier used by persisted browser data and encrypted
 * formats. Keep it assembled here so the retired product name does not leak
 * into current package names, URLs, documentation, or operational resources.
 */
export const PRE_REBRAND_ID = ["file", "sharer"].join("-");
